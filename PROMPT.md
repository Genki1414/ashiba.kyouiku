# Claude Code へ渡す指示文

そのままコピーして貼れる形にしてあります。上から順に使ってください。

---

## 0. 最初に貼るもの（プロジェクトの土台）

```
足場業界向けの教育アプリを本番実装します。

このリポジトリのルートに置いた資料を、先に全部読んでください。
- SPEC.md      技術構成・データモデル・実装順序
- HANDOFF.md   画面の流れ、現場のルール（判定条件）、演出の決まり、残タスク
- prototypes/  章ごとのプロトタイプ（React単一ファイル）

前提として、
- prototypes/ と ashiba-app-v16h.tsx は「仕様の参照用」です。コードは移植せず、
  工程の順序・判定条件・画面構成を読み取る目的で使ってください。
- HANDOFF.md の「現場のルール」は、実際の足場職人が現場基準で決めたものです。
  ここを勝手に一般論へ寄せないでください。判定はこの通りに実装します。
- curriculum.json と narration-all.csv は完成済みのデータです。中身は変えません。

まず、読んだうえで次を出してください。
1. ディレクトリ構成の案
2. データモデルを Supabase のマイグレーションに落としたもの
3. フェーズ1で作るファイルの一覧と、それぞれの責務

実装はまだ始めないでください。上の3点を確認してから進めます。
```

---

## 1. フェーズ1（特別教育の受講画面）

```
フェーズ1を実装してください。

- curriculum.json を読み込み、科目・単元の一覧を表示
- 単元を選ぶと、ナレーション（字幕＋音声）→ 図解 → 災害事例 → 確認問題 の順に進む
- ナレーションは script[] を1行ずつ表示し、読み終えたら次へ
- 視聴時間は再生中のみ加算する。一時停止・離席中は加算しない
- 規定時間（legal_min）に達するまで確認問題を出さない
- 図解は parts[] をタップで開き、全部開くと task に答える
- 災害事例は situation[] を1行ずつ読み、options[] で原因を選ばせてから
  causes[] と prevention[] を表示
- 音声は mp3 を Storage から。未生成のあいだは Web Speech API で代替

デザインは黒基調（#14171B）に黄色（#F5D400）のアクセント。
現場の職人がスマホで使う前提で、文字は大きめに。図はSVGで、縦横比を保って縮小し、
小さい画面でも見切れないようにしてください。

まず1単元が最後まで通ることを確認できる状態にしてください。
```

---

## 2. 実務トレーニングを移植するとき

```
実務トレーニングを実装します。prototypes/ を仕様として読んでください。

作りの順番は、
1. 工程キュー（何を、どの順で、どこに置くか）をデータとして定義する
2. 判定（正しい手／ファールとその理由）を工程キューから導く
3. 描画は最後。プロトタイプの見た目に寄せる

HANDOFF.md の「現場のルール」が判定の仕様です。とくに次は必ず守ってください。
- 段取りは 根がらみ手摺 → 内柱箇所の手摺 → ジャッキ（内柱の分も）
- 柱ごとの順序は 離れ → 水平 → ブラケット（全箇所で固定）
- 支柱を挿す手前で、計算した高さへジャッキのハンドルを合わせる
- 水平器は根がらみ手摺の端から少し中。候補と作業員は進行方向側に出す
- 筋交は下端＝南端側、上端＝出隅側の一方向のみ
- シートは縦張り1スパン1枚、支柱に結び、出隅は隣接2本の後

ファールのときは、何が駄目かではなく「なぜ駄目か」を必ず出してください。
親方は顔のイラストで、普段の顔と怒り顔を描き分けます。
```

---

## 3. 作業中、繰り返し伝えること

```
- 縮尺を合わせてください。1コマ＝450mm、1段＝1,800mm、内柱〜外柱＝600mm、身長＝1,700mm
- 断面図の作業員は、正面向きの絵を横に潰さず、横向きの絵を使ってください
- 画面はスマホ基準。スクロールが必要な画面と、画面内に収める画面を混ぜないでください
- 現場のルールに迷いが出たら、実装を進めず、先に質問してください
```

---

## 4. 質問されたときの答え方（依頼者向けメモ）

Claude Code は現場を知らないので、判断を求められる場面が必ず来ます。
そのときは「効率の問題か、安全の問題か、両方か」を先に伝えると噛み合います。

- 安全の問題　→　ファール（技能点を減らす）
- 効率の問題　→　親方が一言添えるだけ
- 両方　→　ファールにしたうえで、なぜ危ないかを先に言わせる
