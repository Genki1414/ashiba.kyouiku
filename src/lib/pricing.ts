/* 値段の計算。画面にもデータベースにも触らない、ただの計算。
   画面（クライアント）からも読む。

   単価そのものは、ここでは読まない。読むのは src/lib/price.server.ts。
   SEAT_UNIT_PRICE は NEXT_PUBLIC_ ではないので、ブラウザからは見えない。
   ここで読むと、画面に出す金額と実際に請求する金額が食い違う。 */

/** 1人ぶんの受講コードの値段（税抜・円）。仮置き */
export const DEFAULT_UNIT_PRICE = 3000;

/** 設定に書かれた単価を読む。おかしければ仮置きの値。
    環境変数そのものを読むのはサーバだけ（src/lib/price.server.ts） */
export function parseUnitPrice(raw: string | undefined): number {
  const v = Number(raw);
  return Number.isFinite(v) && v >= 0 && `${raw}`.trim() !== "" ? Math.round(v) : DEFAULT_UNIT_PRICE;
}

/* ── 講座ごとの単価 ─────────────────────────

   前は全講座で1つの単価だった。それだと、14時間の職長教育が
   6時間の特別教育と同じ値段になる。講座ごとに持たせる。

   ここに置いてあるのは**決め方**と、既定の値。
   実際にいくらで売るかは、環境変数を読むサーバが決める
   （src/lib/price.server.ts）。この関数は process.env を読まない。
   読ませると、画面から呼んだときに本当の値段が出てしまい、
   「見せている金額」と「請求する金額」が食い違う元になる。 */

/** 既定の単価（税抜・円）。

    ── よその値段（2026年8月に調べた。オンライン受講・税込） ──

      足場の組立て等特別教育（学科6時間）
        茨城教育センター                 8,000円
        中小建設業特別教育協会 WEB講習  10,505円
        労働技能講習協会                11,130円
        → ここ 4,500円（税抜）＝ 4,950円（税込）

      石綿使用建築物等解体等業務（学科4時間30分）
        株式会社斉藤商会 WEB              7,700円
        中小建設業特別教育協会 WEB講習     8,525円
        CIC日本建設情報センター WEB      10,450円
        → ここ 4,500円（税抜）＝ 4,950円（税込）

      高所作業車の運転（学科6時間＋実技3時間。ここで出すのは学科だけ）
        CIC日本建設情報センター Web講座    11,000円（修了証付き 11,500円）
        別のオンライン講習                 10,500円（カード式修了証付き 11,550円）
        学科＋実技を通しでやる講習所         13,000円ほど
          （うち学科だけで 10,000円、実技だけで 3,000円ほど、という内訳）
        → ここ 7,000円（税抜）＝ 7,700円（税込）

        **よそも、オンラインで売っているのは学科だけ。**実技3時間は
        どこも事業者が自社でやる（うちと同じ）。だから 11,000円は
        学科だけの値段で、比べる相手として正しい。
        はじめ 4,500円に置いたが、げんきさんが「安すぎる」と 7,000円に決めた
        （2026年9月2日）。実技の手引きと実施記録の様式が付く
        （/edu/kousho/drill）ぶん、学科だけ売るよそとは中身が違う。

      フルハーネス型墜落制止用器具（学科4時間30分＋実技1時間30分）
        **よそも学科だけをオンラインで売っている。**実技はどこも事業者が自社でやる。
        よその相場は 8,000〜11,000円台。
        → ここ 6,000円（税抜）＝ 6,600円（税込）
        高所作業車と同じく、実技の手引きと実施記録の様式が付く。
        学科が4時間30分（高所作業車は6時間）なので、そのぶん下げてある。

      ロープ高所作業（学科4時間＋実技3時間。ここで出すのは学科だけ）
        **よその値段はまだ調べていない。**フルハーネスと同じに置いた。
        実技3時間は事業者が自社で行う。実技の手引きと実施記録の様式が付く。
        → ここ 6,000円（税抜）＝ 6,600円（税込）

      特定粉じん作業（学科4時間30分・実技なし）
        中小建設業特別教育協会 WEB講習        9,900円
        安全衛生マネジメント協会 WEB講習      9,900円
        CIC日本建設情報センター Web         10,450円（カード型修了証付き）
        → ここ 4,500円（税抜）＝ 4,950円（税込）
        石綿と同じく学科だけで修了まで出せる講座なので、石綿と同じ値段に置いた。

      酸素欠乏・硫化水素危険作業（第2種。学科5時間30分・実技なし）
        中小建設業特別教育協会 WEB講習        9,515円
        （ほかは検索の上限で値段まで取れていない。docs/31）
        → ここ 5,500円（税抜）＝ 6,050円（税込）
        学科だけで修了まで出せる講座だが、石綿・粉じんより1時間長いので、1時間ぶん上げた。

      小型車両系建設機械（整地・運搬・積込み用及び掘削用）（学科7時間＋実技6時間。ここで出すのは学科だけ）
        **よその値段はまだ調べていない**（検索で受講料の頁まで取れていない。docs/29）。
        よそも、オンラインで売っているのは学科だけ。実技6時間はどこも事業者が自社でやる。
        → ここ 7,000円（税抜）＝ 7,700円（税込）
        学科7時間は高所作業車（6時間）より長い。実技の手引きと実施記録の様式が付く
        （/edu/kogata/drill）ので、高所作業車と同じ値段に置いた。

      フォークリフト（最大荷重1トン未満）（学科6時間＋実技6時間。ここで出すのは学科だけ）
        **よその値段はまだ調べていない**（docs/32）。
        よそも、オンラインで売っているのは学科だけ。実技6時間はどこも事業者が自社でやる。
        → ここ 6,000円（税抜）＝ 6,600円（税込）
        学科6時間は高所作業車と同じ。実技の手引きと実施記録の様式が付く（/edu/forklift/drill）。
        高所作業車（7,000円）より受ける人が広く、値段で迷わせたくないので、
        フルハーネス・ロープと同じ 6,000円に置いた。

      テールゲートリフター（学科4時間＋実技2時間。ここで出すのは学科だけ）
        **よその値段はまだ調べていない**（docs/33）。
        → ここ 5,000円（税抜）＝ 5,500円（税込）
        学科4時間は足場（4,500円・3時間）より1時間長い。実技の手引きと実施記録の様式が付く
        （/edu/tailgate/drill）。宅配・配送の会社が人数分まとめて買う教育なので、
        6,000円より下に置いた。

      自由研削用といし（学科4時間＋実技2時間。ここで出すのは学科だけ）
        **よその値段はまだ調べていない**（docs/34）。
        → ここ 5,000円（税抜）＝ 5,500円（税込）
        学科4時間で実技の手引きが付く。テールゲートリフターと同じ作りなので同じ値段に置いた。

      低圧電気取扱（学科7時間＋実技7時間。ここで出すのは学科だけ）
        **よその値段はまだ調べていない**（docs/35）。
        → ここ 7,000円（税抜）＝ 7,700円（税込）
        学科7時間は小型車両系と同じ長さ。実技の手引きと実施記録の様式が付く
        （/edu/teiatsu/drill）ので、小型車両系・高所作業車と同じ値段に置いた。

      巻上げ機（学科6時間＋実技4時間。ここで出すのは学科だけ）
        **よその値段はまだ調べていない**（docs/36）。
        → ここ 6,000円（税抜）＝ 6,600円（税込）
        学科6時間はフォークリフト・高所作業車と同じ長さ。実技の手引きと実施記録の様式が付く
        （/edu/winch/drill）。フォークリフトと同じ 6,000円に置いた。

      ローラー（学科6時間＋実技4時間。ここで出すのは学科だけ）
        **よその値段はまだ調べていない**（docs/37）。
        → ここ 6,000円（税抜）＝ 6,600円（税込）
        学科6時間はフォークリフト・巻上げ機と同じ長さ。実技の手引きと実施記録の様式が付く
        （/edu/roller/drill）。フォークリフト・巻上げ機と同じ 6,000円に置いた。

      チェーンソー（学科9時間＋実技9時間。ここで出すのは学科だけ）
        **よその値段はまだ調べていない**（docs/38）。
        → ここ 8,000円（税抜）＝ 8,800円（税込）
        **学科9時間はうちでいちばん長い**（低圧電気の7時間より2時間長い）。
        実技9時間の手引きと実施記録の様式が付く（/edu/chainsaw/drill）。
        職長（14時間・討議つき）の 7,000円より高いのは、学科の長さと事例27件の量による。

      アーク溶接（学科11時間＋実技10時間。ここで出すのは学科だけ）
        **よその値段はまだ調べていない**（docs/39）。
        → ここ 9,000円（税抜）＝ 9,900円（税込）
        **学科11時間はうちでいちばん長い**（チェーンソーの9時間より2時間長い）。
        実技10時間の手引きと実施記録の様式が付く（/edu/arc/drill）。
        科目3（作業の方法）だけで6時間あり、事例23件を入れている。

      機械研削用といし（学科7時間＋実技3時間。ここで出すのは学科だけ）
        **よその値段はまだ調べていない**（docs/40）。
        → ここ 7,000円（税抜）＝ 7,700円（税込）
        学科7時間は低圧電気・小型車両系と同じ長さ。実技の手引きと実施記録の様式が付く
        （/edu/kikaitoishi/drill）。自由研削（4時間・5,000円）より3時間長いので上に置いた。

      ショベルローダー等（最大荷重1トン未満）（学科6時間＋実技6時間。ここで出すのは学科だけ）
        **よその値段はまだ調べていない**（docs/41）。
        → ここ 6,000円（税抜）＝ 6,600円（税込）
        学科6時間・実技6時間で、フォークリフトと同じ形。中身は違うが受ける人の広さも近いので、
        **フォークリフトと同じ 6,000円**に置いた。値段で迷わせないためでもある。
        実技の手引きと実施記録の様式が付く（/edu/shovel/drill）。

      職長・安全衛生責任者教育（14時間・討議つき）
        中小建設業特別教育協会 WEB講習  17,600円
          （動画12時間18分＋Zoom討議2時間30分。うちと同じ作り）
        → ここ 7,000円（税抜）＝ 7,700円（税込）

    JAC（建設技能人材機構）が特定技能外国人向けに無料の
    オンライン特別教育をやっているが、あれは会員向けの受入支援で、
    誰でも申し込める講習ではない。値段の比べる相手には入れていない。

    変えるときは環境変数で。ここを書き換えると上げ直すまで直らない。 */
export const DEFAULT_COURSE_PRICE: Record<string, number> = {
  ashiba: 4500,
  shokucho: 7000,
  ishiwata: 4500,
  kousho: 7000,
  harness: 6000,
  rope: 6000,
  funjin: 4500,
  sanketsu: 5500,
  kogata: 7000,
  forklift: 6000,
  tailgate: 5000,
  toishi: 5000,
  teiatsu: 7000,
  winch: 6000,
  roller: 6000,
  chainsaw: 8000,
  arc: 9000,
  kikaitoishi: 7000,
  shovel: 6000,
};

/** 講座の目印から環境変数の名前を作る。ashiba → SEAT_UNIT_PRICE_ASHIBA */
export const priceEnvName = (courseId: string): string =>
  `SEAT_UNIT_PRICE_${courseId.replace(/[^A-Za-z0-9]/g, "_").toUpperCase()}`;

/** その講座の単価を決める。環境変数の値は、読んだ側から渡す。

    ① その講座だけの設定（SEAT_UNIT_PRICE_◯◯）
    ② 上の表の既定値
    ③ 全講座ぶんの設定（SEAT_UNIT_PRICE）
    ④ 仮置きの値
    の順。 */
export function pickUnitPrice(a: {
  courseId?: string;
  /** SEAT_UNIT_PRICE_◯◯ の値 */
  own?: string;
  /** SEAT_UNIT_PRICE の値 */
  all?: string;
}): number {
  const id = (a.courseId ?? "").trim();
  if (id) {
    if (`${a.own ?? ""}`.trim() !== "") return parseUnitPrice(a.own);
    const set = DEFAULT_COURSE_PRICE[id];
    if (typeof set === "number") return set;
  }
  return `${a.all ?? ""}`.trim() !== "" ? parseUnitPrice(a.all) : DEFAULT_UNIT_PRICE;
}

/** 消費税 */
export const TAX_RATE = 0.1;

/** 一度に買える人数の上限。押し間違いで桁を増やさないため */
export const MAX_SEATS = 500;

export type Quote = {
  seats: number;
  /** 税抜の単価 */
  unitPrice: number;
  /** 税抜の小計 */
  subtotal: number;
  /** 消費税（円・切り捨て） */
  tax: number;
  /** 税込の合計。orders.amount に入る */
  total: number;
};

/** 人数から金額を出す。単価は必ず渡す（サーバが持っている値）。
    人数がおかしければ null */
export function quote(seats: number, price: number): Quote | null {
  if (!Number.isFinite(price) || price < 0) return null;
  if (!Number.isInteger(seats) || seats < 1 || seats > MAX_SEATS) return null;
  const subtotal = price * seats;
  const tax = Math.floor(subtotal * TAX_RATE);
  return { seats, unitPrice: price, subtotal, tax, total: subtotal + tax };
}

/** 「1,234円」 */
export const yen = (n: number): string => `${n.toLocaleString("ja-JP")}円`;

/** 請求書払いの支払期限。月末締め翌月末払いに寄せて、30日後の月末 */
export function dueDate(from: Date): Date {
  const d = new Date(from.getTime());
  d.setDate(d.getDate() + 30);
  /* その月の末日 */
  return new Date(d.getFullYear(), d.getMonth() + 1, 0);
}
