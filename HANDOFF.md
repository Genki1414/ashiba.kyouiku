# 足場トレーニング 引き継ぎ（Claude Code 用）

このドキュメントは、チャットで作ったプロトタイプを本番実装へ渡すためのものです。
既存の `SPEC.md`（技術構成・データモデル・実装順序）と併せて読んでください。
ここには **SPEC.md 以降に決まった現場のルールと画面の作り** をまとめています。

---

## 1. 現在のファイル

| ファイル | 中身 |
|---|---|
| `ashiba-app-v16h.tsx` | **統合アプリ**。第1〜3章＋特別教育＋決済＋管理＋資材カタログ＋通し見学 |
| `ashiba-ch2-v6.tsx` | 第2章 単体 |
| `ashiba-ch3-v13.tsx` | 第3章 単体 |
| `ashiba-glossary-v9.tsx` | 資材カタログ 単体 |
| `ashiba-demo-v8.tsx` | 通し見学 単体 |
| `ashiba-suihei-v2.tsx` | 水平器の置き場所の場面（統合済み・単体でも動く） |
| `curriculum.json` / `narration-all.csv` | 特別教育の教材（変更なし） |

統合アプリの構造は、モジュールごとに即時関数で囲って名前空間を分けています
（`TrainingApp` / `Ch2App` / `Ch3App` / `GlossaryApp` / `DemoApp` / `EduApp` / `CheckoutApp` / `AdminApp`）。
本番では素直にコンポーネント分割してください。

---

## 2. 画面の流れ

```
ホーム
 └ 実務トレーニング → 章選択
      ├ 第1章 → 資材カタログ → 通し見学（15手）→ チュートリアル/本番
      ├ 第2章
      ├ 第3章
      └ 第4〜6章（準備中）
 └ 特別教育（学科6時間）
 └ お申込み・決済 / 管理
```

- 章の中では上部バーの「資材」からカタログをいつでも開ける
- 第1章・第2章は縦に長いので**ページごとスクロール**、第3章と見学・カタログは**画面内に収める**
- チュートリアル／本番のモード切替あり（本番は親方に聞く・手順書が消え、設置箇所のゴーストが薄くなる）

---

## 3. 現場のルール（判定に使う）

### 第1章 段取りと根がらみ

1. **段取りの順序**：根がらみ手摺を並べる → 内柱箇所の手摺 → ジャッキを配る（内柱の分も）
   - 根がらみ手摺が1本も無い状態でジャッキ・内柱手摺を置くとファール
   - 理由：手摺が割り付けの現物合わせになる。無いとジャッキの位置が決まらない
2. **内柱**：端部は必ず。中間は2スパンに1本。理由は端部に内柱が無いと足場が安定しないから
3. **ジャッキ合わせ**：支柱を挿す手前で、足場の高さ計算から出した高さへ**ハンドル**を合わせる
   - ジャッキの全長は変わらない。ネジ棒に沿ってハンドルだけが上下し、その高さに支柱の後端が乗る
   - 高さの計算は積算アプリで出せる
   - この場面はゲーム中2回だけ表示
4. **建方**：基準は出隅 → 2方向に根がらみ手摺 → 両隣の柱
   - 根がらみ手摺は立っている柱のコマへ先に入れる（柱 → 手摺 → 次の柱）
   - 名前は根がらみ手摺だが、使う資材は手摺資材。落下防止手摺・上さん・中さんと同じ物
5. **柱ごとの順序（全箇所で固定）**：離れ → 水平 → ブラケット
6. **水平器の置き場所**：根がらみ手摺の**端から少し中**
   - 端は差し込みの都合で凹んでいて面が出ない → 気泡が中央でも水平は出ていない
   - 中ほどはジャッキから遠く、回しながら気泡が見えない
   - 置き場所の候補と作業員は**進行方向側**に出す
   - 実地で起きた失敗：端の凹みで測ったため、4面が一周したときに高さが合わなかった
7. **内柱の箇所**：内柱を立てる → 踏板高さの600手摺でつなぐ → 離れ → 水平（水平器は**支柱**に当てる）
8. **ブラケット**：外柱から**建物側**へ張り出す。そこに踏板が載る

### 第2章 高所作業

- 地上から筋交 → 昇降階段で1段目 → 安全帯を支柱に → 1段目の手摺（荷揚げ側から）→ 安全帯を手摺へ
  → 支柱・内柱を継ぐ（奥から）→ ブラケット／踏板手摺 → 壁当てジャッキ → 踏板 → 1段目から筋交
  → 2段目へ → 2段目の手摺 → 2段目から筋交 → 屋根 → 転落防止手摺（中さん2,250 → 上さん2,700）
- **筋交**：向きは一方向のみ。下端＝南端側、上端＝出隅側。1段につき1本
  （地上＝南端〜南②、1段目＝南①〜南②、2段目＝出隅〜南①で一直線に揃う）
  - 上のコマに先端を入れてから、後端を下のコマへ振り下ろす
- **壁当てジャッキ**：付く相手は踏板ではなく**踏板手摺**。踏板手摺のすぐ下のコマ
  - 低い位置に付けると踏板を歩く者の足に当たる
  - 回す操作は根がらみのジャッキと同じ左右ボタン
  - 盤面では横一直線ではなく**斜め**（奥行き方向）に描く

### 第3章 火打とシート

- **火打**：出隅4箇所（南東・南西・北西・北東）。4面組立済みの状態を平面図で展開
  - 足場と**二等辺三角形**になるように掛ける。**支柱**に付ける
  - 基本的に手摺には付けない。やむを得ず手摺の場合は、その手摺に抜け止め措置
  - 目的：平面内でひし形に変形するのを防ぐ。圧縮材と併用して引張効果（圧縮材は説明のみ）
- **シート**：縦張り、1スパンに1枚、重ねしろ無し
  - まず全スパンを最上段から下へ垂らす。広げるときは**足で挟む**（落とすのが一番多い失敗）
  - そのあと**支柱**に結ぶ。緊結ピッチは450または900（戸建は900でよい）
  - 段単位。2段目の全支柱 → 1段目 → 地上（ゲームでは2段目まで。以降は繰り返しなので省略）
  - 支柱ごとの結束は**上から下**（4コマ目 → … → 1コマ目）。位置の表記は「コマ」
  - 立っている踏板の高さ（0コマ）は候補に出すが選ぶとファール。踏板高さは下の段から結ぶ方が効率が良い
  - **出隅は最後**。隣接する2本（南①・西①）を結んでから。先に結ぶと隣の支柱側に隙間

---

## 4. 演出の決まり

- 親方は**顔のイラスト**で出す。普段の顔と怒り顔を描き分ける
- 叱るときは顔の横にセリフ（なぜ駄目かが分かる文言）を必ず添える
- 作業させる場面は「なぜそうするのか」を強調する（通し見学は全15手に理由を表示済み）
- 縮尺は合わせる。1コマ＝450mm、1段＝1,800mm、内柱〜外柱＝600mm、身長＝1,700mm
  - 断面図の作業員は正面向きを潰さず、横向きの絵を使う
- 図はSVGで縦横比を保って縮小し、見切れさせない

---

## 5. 残っていること

- 実務トレーニング 第4〜6章（本足場／壁つなぎ・層間ネット／技能士試験の実技）
- ハンマーで叩く別モード
- 監修者の選定と台本の監修、所轄労働局への構成の照会
- 利用規約・プライバシーポリシー（顔特徴データ＝個人識別符号）
- 音声収録（1,683行）、図解74枚
- 本番実装（Next.js + Supabase + Stripe。`SPEC.md` のフェーズ1から）
