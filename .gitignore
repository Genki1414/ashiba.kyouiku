node_modules/
.next/
# 試験でもう1台立てるときの置き場所（NEXT_DIST_DIR）。
# 名前を増やすたびに書き足していたら、.next-tokubetsu を
# 94MB ぶん commit してしまった（2026-09-09）。まとめて見ない
.next-*/
out/
*.tsbuildinfo
next-env.d.ts

# 鍵の入ったファイル。**このリポジトリは公開されている。**
# 前は .env と .env.local の2つだけを書いていたが、
# vercel env pull は行き先の名前を自由に付けられる（.env.production など）。
# 名前を1文字変えただけで commit できてしまうので、.env で始まる物は全部見ない。
# 例が要るときは .env.example（鍵の値を入れないこと）
.env*
!.env.example
.vercel/

# 画面の試験が置いていく写し（SC で行き先を変えられる）
/*.png
